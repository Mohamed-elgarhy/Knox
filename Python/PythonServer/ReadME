in order to start the python server just copy the 2 files and from command line terminal type the command :

python main

after starting python server , any request should be written in file called demofile.txt
